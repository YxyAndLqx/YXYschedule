# 数据结构
## 用面向对象方法和C++语言描述
***
2020-4-16
***
### 第一章 数据结构概论
#### 1.1 数据结构的概念
##### 1.1.1 数据结构举例
##### 1.1.2 数据与数据结构
##### 1.1.3 数据结构的分类
1.线性结构
2.非线性结构
##### 1.1.4 数据结构课程的内容
#### 1.2 数据结构的抽象形式
##### 1.2.1 数据结构的抽象形式
##### 1.2.2 数据结构与抽象数据类型
其实就是类
#### 1.3 作为ADT的C++类
##### 1.3.1 面向对象的概念
1. 什么是面向对象：面向对象=对象+类+继承+消息通信
2. 对象是指在应用问题中出现的各种实体、事件、规格说明等，它是由一组属性值和在这组值上的一组服务（或称操作）构成。
3. 可以通过对象的服务来改变该对象的属性值，即对象的状态。
4. 引入继承机制是面向对象方法的最具特色的方面。
5. 继承的好处是可以合理地将各个对象的属性和服务分配到所有的类中，减少数据存储和程序代码的重复。
***
2020-4-19
***
##### 1.3.2 C++中的类
1. C++中对于面向对象程序设计的支持，核心部分就是类的定义。
2. 程序中其他类的对象或操作都能请求该类的对象执行它们，因此，这些数据成员和成员函数构成类的接口部分。
3. 在private域声明的成员仅允许该类的成员函数或友元函数存取。
4. 在C++中struct与class的区别在于：在struct中，默认的访问级别是public。
5. 若在struct内部自始至终缺省访问级别，则所有的成员都在公共接口中。而在class中，默认的访问级别是private。除此之外，struct与class是等价的。
6. 与struct相似，union中默认存取级别是public。
##### 1.3.3 C++中的对象  
__1.建立类的对象__  
建立类的对象也称为实例化，采用的方式类似于定义C变量的方式，可以自动地或静态地或通过动态分配来建立。  
__2.构造函数__  
当定义实例时给定初始值，则该实例以给定初始值来初始化其数据成员.  
__3.析构函数__  
1. 当要撤销对象时，需要隐式地调用另一个函数，叫做析构函数。
2. 一个类可以定义几个构造函数但是只能定义一个析构函数。
3. 一个析构函数用于在删除一个类的对象时做清除工作。
##### 1.3.4 C++的输入输出
1. 在C++语言程序中，基本的输入输出方式有两种：键盘屏幕输入输出和文件输入输出。  
__1.键盘屏幕输入输出__  
2. 在C++中用cin、cout和cerr来定义键盘输入类、屏幕输出类和错误信息输出类。  
__2.文件输入输出__
***
2020-4-20
***
##### 1.3.5 C++ 中的函数
__1.C++ 函数的概念__
1. 在C++中有两种函数：常规函数和成员函数。
2. 不论哪一种函数，其定义都包含4个部分：函数名、形式参数表、返回类型和函数体。
3. 在C++中所有函数都有一个返回值，或者返回计算结果或者返回执行状态。
4. 如果函数不需要返回值可以使用void来表示它的返回类型。
5. 函数返回时可以通过引用方式。  
__2.C++中的参数传递__
6. 函数调用时传送给形参表的实参必须与形参在类型、个数、顺序上保持一致。
7. 参数传递有两种方式：一种是传值，这是默认的参数传递方式；另一种是引用类型。
8. 使用引用类型方式传递参数时，须将形参声明为引用类型，即在参数名前加一个“&”。
9. 函数执行后实参的值将发生变化。
10. 在把一个体积较大的对象作为参数传递时。使用引用方式将大大节省传递参数的时间，并可节省存储参数对象的副本空间。
11. 当一个函数的返回值多于一个时，其中一个可由return语句返回，其他的返回值可以使用引用型参数返回。
12. 在使用传值型参数时，参数可以是常数、常量、变量或表达式，但是使用引用型参数时，参数只能是变量或对象。
13. 在函数体中不能修改常值参数。  
__3.成员函数的返回值__
1. 成员函数的返回值为传值方式时，允许改变该对象的私有数据成员。
2. 当成员函数的放回值为常值传值方式时，需要在函数说明中加上const标识，使得该对象的私有成员不能被改变。  
__4.友元函数__  
1. 如果想通过这种函数存取类的私有成员和保护成员，则必须在类的声明中给出函数的原型，并在该函数原型前面加上一个friend。


